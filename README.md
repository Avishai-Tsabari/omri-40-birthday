# חפש את המטמון — עומרי בן 40

אתר סטטי ליום ההולדת (27.08.2026). בלי שרת, בלי בנייה, בלי התקנות.

**המסלול:**

1. שולחים לעומרי את הלינק **בבוקר**.
2. הוא פותח → **ברכה מונפשת** (קונפטי, 40 בזהב).
3. אחרי כמה שניות → **ספירה לאחור ל-17:00** (שעון ישראל, לא משנה איפה הטלפון שלו).
4. ב-17:00 מופיע כפתור → **סרטון הפתיחה** נפתח ומגלה את התחנה הראשונה.
5. משם: קוד מהאחיין → סרטון נבל → התחנה הבאה. חמש פעמים.
6. **3 טעויות ברצף** → נפתח סרטון של אחיין עם ברכה ו"הקוד שגוי", ואז חוזרים לנסות.

אם הוא פותח את הלינק אחרי 17:00 — הברכה רצה ומיד אחריה נפתח כפתור הפתיחה.
אם הוא סוגר וחוזר באמצע המשחק — הוא ממשיך בדיוק מאיפה שהיה.

## הרצה מקומית

```bash
cd C:\code\agentic\omri-40-birthday
python serve.py
```

ואז `http://localhost:8080`.

שתי הערות:
- **לא** להשתמש ב-`python -m http.server`. הוא לא תומך ב-Range requests, ובלי זה
  הדפדפן מסרב לנגן וידאו — הסרטון נתקע על מסך שחור. `serve.py` פותר בדיוק את זה.
  אירוח אמיתי (Netlify / Vercel / Pages) תומך בזה מעצמו.
- פתיחה של `index.html` ישירות מהדיסק תעבוד חלקית — הסרטונים עלולים להיחסם.

## העלאה לאוויר

התיקייה כולה סטטית, אז כל אחד מאלה עובד:

- **Netlify Drop** — לגרור את התיקייה ל-https://app.netlify.com/drop. הכי מהיר.
- **Vercel** — `npx vercel --prod` מתוך התיקייה.
- **GitHub Pages** — לדחוף לריפו ולהפעיל Pages.

**חשוב:** לפתוח את הכתובת בטלפון של עומרי **לפני** האירוע, לוודא שהסרטונים מתנגנים,
ולשמור אותה כסימנייה במסך הבית. לא רוצים לחפש לינק בחמש אחר הצהריים באמצע הרחוב.

## החלפת הסרטונים

בתיקייה `videos/` יש כרגע **סרטוני placeholder** (מלבן צבעוני, 10 שניות).
פשוט להחליף כל קובץ באותו שם:

| קובץ | הנבל | מגלה את התחנה |
|---|---|---|
| `videos/a1.mp4` | סרטון הפתיחה — הגמל (17:00) | פארק שרונה |
| `videos/a2.mp4` | **סרטון ההגעה** (נפתח לפי מיקום) | רחבת מתקני הספורט |
| `videos/a3.mp4` | שרדר | רחבת המשחקייה |
| `videos/a4.mp4` | דוקטור אגמן | רחבת הגלגיליות |
| `videos/a5.mp4` | סקאר | כיכר המושבה · טחנת הרוח |
| `videos/a6.mp4` | קפטן הוק | גן המשחקים שליד שרונה מרקט |
| `videos/a7.mp4` | באטמן (סיום) | טונינו |
| `videos/x0.mp4` | אחיין — ברכה + "הקוד שגוי" | נפתח אחרי 3 טעויות |

`a1.mp4` הוא הגמל האמיתי (Gamal-1). **שאר הקבצים הם עותקים שלו** כ-placeholder, כדי שאפשר יהיה לבדוק את המסלול
מקצה לקצה. מחליפים קובץ-קובץ כשמגיעים הסרטונים האמיתיים — בלי לגעת
בקונפיג, השמות כבר מחווטים.

השמות אטומים בכוונה — אם עומרי יציץ בכתובת של הסרטון, הוא לא ילמד מזה כלום.

**פורמט מומלץ:** MP4 / H.264 / AAC, אנכי 9:16, עד ~10 שניות, עד ~10MB לקובץ.
זה מה שמתנגן בלי בעיות באייפון ובאנדרואיד.

אפשר גם להוסיף תמונת פתיחה לכל סרטון: `posters/a1.jpg` וכו'. לא חובה.

## שינוי תוכן

**כל מה שמשנים נמצא ב-`config.js` בלבד.** אין צורך לגעת בקוד.

- טקסטים, שמות תחנות, שמות נבלים — שם.
- להוסיף או להוריד תחנה — להוסיף/למחוק אובייקט במערך `stops`. הכל מסתדר לבד.
- **מעבר בין מקומות** — לשנות את `next.place` ואת `next.text`. זהו.

### המפה

```js
map: { label: 'מפה', url: 'https://parksarona.com/.../sarona_map_heb.jpg' }
```

לינק בתחתית המסך, זמין לעומרי **בכל שלב של המשחק** (לא בברכה ובספירה —
שם עוד אין מה לחפש). נפתח בלשונית חדשה, כך שהוא לא מאבד את מקומו במשחק.
`url: null` מסתיר את הכפתור לגמרי. מחליפים כאן אם עוברים לנמל.

### שעת ההתחלה

```js
countdown: { startsAt: '2026-08-27T17:00:00+03:00' }
```

ה-`03:00+` הוא מה שמבטיח שהספירה נכונה גם אם הטלפון של עומרי באזור זמן אחר.
אם השעה זזה — משנים כאן, וזה נקרא מחדש בכל שנייה.

### ⚠️ מצב בדיקות — המתג שחייבים לזכור

```js
testMode: true    →    false
```

בראש `config.js`. כשהוא דלוק נחשפים שני קיצורים:
- כפתור **"דלג על הספירה (בדיקה)"** במסך הספירה
- כפתור **"הגעתי לפארק"** מופיע מיד בשער ההגעה, בלי להמתין 45 שניות

**להעביר ל-`false` לפני ששולחים את הלינק לעומרי**, ולהעלות את `?v=` ב-`index.html`.
זה מתג אחד לשני הקיצורים — אין עוד מה לזכור.

בכיבוי, כפתור "הגעתי לפארק" עדיין מופיע כשההרשאה נדחית או אחרי 45 שניות.
זו רשת ביטחון אמיתית, לא קיצור בדיקה.

### חשיפת המתנה

```js
revealGiftInApp: false
```

`false` = מסך הסיום רק שולח למסעדה.
`true` = מסך הסיום חושף את המדפסת. אפשר לשנות אפילו במהלך הערב (צריך רענון דף).

## הקודים

המקום: **פארק שרונה**.

| # | שעה | האחיין | הנבל | התחנה | הקוד שהוא מוסר | הקוד מגלה |
|---|---|---|---|---|---|---|
| 1 | 18:00 | מעיין (8) | שרדר | רחבת מתקני הספורט | `שרדר2018` | רחבת המשחקייה |
| 2 | 18:12 | אריאל (7) | דוקטור אגמן | רחבת המשחקייה | `אגמן2018` | רחבת הגלגיליות |
| 3 | 18:24 | ליב (4) | סקאר | רחבת הגלגיליות | `סקאר2022` | כיכר המושבה · טחנת הרוח |
| 4 | 18:36 | אורי (3) | קפטן הוק | כיכר המושבה · טחנת הרוח | `קפטןהוק2023` | גן המשחקים שליד שרונה מרקט |
| 5 | 18:48 | תום (2) + כולם | ג'פאר → באטמן | גן המשחקים שליד שרונה מרקט | `ג'פאר2024` | **טונינו** (19:00) |

לפני התחנות האלה יש שני שלבים בלי קוד: **17:00** נפתח סרטון הגמל ושולח אותו
לפארק, ואז **שער ההגעה** — ראה למטה.

המסלול נע ממזרח למערב ומסתיים צעד אחד מטונינו. הקוד החמישי פותח את סיום
הבאטמן, ובמסך הסיום לינק ה**מפה** מצביע על טונינו במקום על מפת הפארק.

את התחנה הראשונה.
5. משם: קוד מהאחיין → סרטון נבל → התחנה הבאה. חמש פעמים.
6. **3 טעויות ברצף** → נפתח סרטון של אחיין עם ברכה ו"הקוד שגוי", ואז חוזרים לנסות.

אם הוא פותח את הלינק אחרי 17:00 — הברכה רצה ומיד אחריה נפתח כפתור הפתיחה.
אם הוא סוגר וחוזר באמצע המשחק — הוא ממשיך בדיוק מאיפה שהיה.

## הרצה מקומית

```bash
cd C:\code\agentic\omri-40-birthday
python serve.py
```

ואז `http://localhost:8080`.

שתי הערות:
- **לא** להשתמש ב-`python -m http.server`. הוא לא תומך ב-Range requests, ובלי זה
  הדפדפן מסרב לנגן וידאו — הסרטון נתקע על מסך שחור. `serve.py` פותר בדיוק את זה.
  אירוח אמיתי (Netlify / Vercel / Pages) תומך בזה מעצמו.
- פתיחה של `index.html` ישירות מהדיסק תעבוד חלקית — הסרטונים עלולים להיחסם.

## העלאה לאוויר

התיקייה כולה סטטית, אז כל אחד מאלה עובד:

- **Netlify Drop** — לגרור את התיקייה ל-https://app.netlify.com/drop. הכי מהיר.
- **Vercel** — `npx vercel --prod` מתוך התיקייה.
- **GitHub Pages** — לדחוף לריפו ולהפעיל Pages.

**חשוב:** לפתוח את הכתובת בטלפון של עומרי **לפני** האירוע, לוודא שהסרטונים מתנגנים,
ולשמור אותה כסימנייה במסך הבית. לא רוצים לחפש לינק בחמש אחר הצהריים באמצע הרחוב.

## החלפת הסרטונים

בתיקייה `videos/` יש כרגע **סרטוני placeholder** (מלבן צבעוני, 10 שניות).
פשוט להחליף כל קובץ באותו שם:

| קובץ | הנבל | מגלה את התחנה |
|---|---|---|
| `videos/a1.mp4` | סרטון הפתיחה (נפתח ב-17:00) | שרונה מרקט |
| `videos/a2.mp4` | שרדר | רחבת הגלגיליות |
| `videos/a3.mp4` | אגמן | רחבת המשחקים |
| `videos/a4.mp4` | סקאר | רחבת המוזיקה |
| `videos/a5.mp4` | קפטן הוק | כיכר המושבה |
| `videos/x0.mp4` | אחיין — ברכה + "הקוד שגוי" | נפתח אחרי 3 טעויות |

`a1.mp4` הוא הגמל האמיתי (Gamal-1). **שאר הקבצים הם עותקים שלו** כ-placeholder, כדי שאפשר יהיה לבדוק את המסלול
מקצה לקצה. מחליפים קובץ-קובץ כשמגיעים הסרטונים האמיתיים — בלי לגעת
בקונפיג, השמות כבר מחווטים.

השמות אטומים בכוונה — אם עומרי יציץ בכתובת של הסרטון, הוא לא ילמד מזה כלום.

**פורמט מומלץ:** MP4 / H.264 / AAC, אנכי 9:16, עד ~10 שניות, עד ~10MB לקובץ.
זה מה שמתנגן בלי בעיות באייפון ובאנדרואיד.

אפשר גם להוסיף תמונת פתיחה לכל סרטון: `posters/a1.jpg` וכו'. לא חובה.

## שינוי תוכן

**כל מה שמשנים נמצא ב-`config.js` בלבד.** אין צורך לגעת בקוד.

- טקסטים, שמות תחנות, שמות נבלים — שם.
- להוסיף או להוריד תחנה — להוסיף/למחוק אובייקט במערך `stops`. הכל מסתדר לבד.
- **מעבר בין מקומות** — לשנות את `next.place` ואת `next.text`. זהו.

### המפה

```js
map: { label: 'מפה', url: 'https://parksarona.com/.../sarona_map_heb.jpg' }
```

לינק בתחתית המסך, זמין לעומרי **בכל שלב של המשחק** (לא בברכה ובספירה —
שם עוד אין מה לחפש). נפתח בלשונית חדשה, כך שהוא לא מאבד את מקומו במשחק.
`url: null` מסתיר את הכפתור לגמרי. מחליפים כאן אם עוברים לנמל.

### שעת ההתחלה

```js
countdown: { startsAt: '2026-08-27T17:00:00+03:00' }
```

ה-`03:00+` הוא מה שמבטיח שהספירה נכונה גם אם הטלפון של עומרי באזור זמן אחר.
אם השעה זזה — משנים כאן, וזה נקרא מחדש בכל שנייה.

### ⚠️ מצב בדיקות — המתג שחייבים לזכור

```js
testMode: true    →    false
```

בראש `config.js`. כשהוא דלוק נחשפים שני קיצורים:
- כפתור **"דלג על הספירה (בדיקה)"** במסך הספירה
- כפתור **"הגעתי לפארק"** מופיע מיד בשער ההגעה, בלי להמתין 45 שניות

**להעביר ל-`false` לפני ששולחים את הלינק לעומרי**, ולהעלות את `?v=` ב-`index.html`.
זה מתג אחד לשני הקיצורים — אין עוד מה לזכור.

בכיבוי, כפתור "הגעתי לפארק" עדיין מופיע כשההרשאה נדחית או אחרי 45 שניות.
זו רשת ביטחון אמיתית, לא קיצור בדיקה.

### חשיפת המתנה

```js
revealGiftInApp: false
```

`false` = מסך הסיום רק שולח למסעדה.
`true` = מסך הסיום חושף את המדפסת. אפשר לשנות אפילו במהלך הערב (צריך רענון דף).

## הקודים

המקום: **פארק שרונה**.

| # | האחיין | התחנה שבה הוא עומד | הקוד שהוא מוסר | הקוד מגלה |
|---|---|---|---|---|
| 1 | מעיין (8) | שרונה מרקט | `שרדר2018` | רחבת הגלגיליות |
| 2 | אריאל (7) | רחבת הגלגיליות | `אגמן2018` | רחבת המשחקים |
| 3 | ליב (4) | רחבת המשחקים | `סקאר2022` | רחבת המוזיקה |
| 4 | אורי (3) | רחבת המוזיקה | `קפטןהוק2023` | כיכר המושבה |
| 5 | תום (2) | כיכר המושבה | `ג'פאר2024` | ארוחת ערב (סיום) |

## שער ההגעה (גיאולוקציה)

השלב היחיד שלא נפתח בקוד מאחיין:

1. **17:00** — הספירה נגמרת, נפתח סרטון הגמל. עומרי בבית, רחוק מהפארק.
   הסרטון שולח אותו לפארק שרונה.
2. **בדרך** — מסך "לך לפארק שרונה" שמראה מרחק חי ("אתה במרחק 1.8 ק\"מ").
3. **בהגעה** — כשהוא בתוך הרדיוס: "הגעת." וכפתור שפותח את הסרטון השני,
   וזה מגלה את תחנה 1. משם הכל בקודים.

```js
arrival: { lat: 32.07337209774463, lng: 34.784830323066764, radiusMeters: 300 }
```

**הגיאו הוא תוספת, לא תנאי.** כפתור **"הגעתי לפארק"** מופיע מיד אם ההרשאה
נדחתה או שהאיתור נכשל, ובכל מקרה אחרי 45 שניות. עומרי לא ייתקע ברחוב בגלל GPS.

בזמן שהוא בשער, אף כתובת אחרת לא נגישה — גם לא מסך הקוד. נבדק: `#/code`,
`#/wrong`, `#/stop/2`, `#/stop/5` ו-`#/finale` כולם מוחזרים ל-`#/travel`.
גבול הרדיוס נבדק גם הוא: 450 מ' ו-320 מ' נשארים סגורים, 250 מ' פותח.

ההשוואה **סלחנית**: לא רגישה לרישיות, לרווחים, לגרשיים, לניקוד או לאותיות
סופיות. `ג'פאר2024`, `גפאר2024` ו-`ג׳פאר 2024` — כולם עוברים. מקובל גם שם
הנבל לבדו בלי השנה.

**אי אפשר לקפוץ קדימה.** קוד של תחנה עתידית לא יתקבל, גם אם ינחש נכון.
גם `2018` לבדו לא יתקבל — ראה הערה למטה.

> **למה שנה לבדה לא עובדת:** יש בקוד התאמת-ברקוד שמסתפקת ב-4 הספרות
> האחרונות, בשביל המקרה שקוד יהיה ברקוד של צעצוע. `שרדר2018` ו-`אגמן2018`
> חולקים את אותה שנה, אז ההתאמה הזו הייתה פותחת את אגמן עם הקוד של שרדר.
> לכן היא מוגבלת עכשיו לקודים שהם ספרות בלבד. אם תוסיפו קוד-ברקוד אמיתי,
> הוא ימשיך לעבוד עם 4 ספרות אחרונות.

## רשתות ביטחון לערב עצמו

- **הסרטון לא נטען?** האתר מציג את התמלול של הנבל במקום. המשחק ממשיך.
- **הסרטון נתקע?** אחרי 3 שניות מופיע כפתור "דלג לרמז".
- **הדף נסגר או הטלפון מת?** ההתקדמות נשמרת. פתיחה מחדש חוזרת לאותו מקום.
- **תפריט מפעיל** — **לחיצה ארוכה (כשנייה) על הכותרת** פותחת רשימה של כל התחנות,
  ואפשר לקפוץ לכל אחת מהן. זה למקרה שמשהו משתבש בשטח. אל תדגים את זה לעומרי.
- **איפוס** — בתוך תפריט המפעיל. מחזיר גם את הברכה ואת הספירה.
- **דילוג על הספירה** — הכי פשוט הכפתור שב-`testMode`. לחלופין `?skip=1` בכתובת:
  `http://localhost:8080/?skip=1`. הספירה מיד "בשלה" וכפתור הפתיחה מופיע.
  הפרמטר **נשאר** בכתובת בכוונה, כדי שרענון וניווט ימשיכו לדלג.
  **אל תשלח לעומרי לינק עם `skip=1`** — הוא יוכל להתחיל לפני הזמן.
- **חזרה להתחלה בזמן בדיקות** — להוסיף `?reset=1`. מנקה הכל ומתחיל מהברכה.
  הפרמטר מוסר מהכתובת מיד אחרי (ורק הוא — `skip=1` נשמר), כך שרענון
  באמצע המשחק לא יאפס בטעות.
- שניהם יחד: `http://localhost:8080/?reset=1&skip=1` — זה הקיצור לבדיקה
  מלאה מההתחלה.
- **עדכנת קובץ אחרי שכבר שלחת את הלינק?** צריך להעלות את `?v=` ב-`index.html`
  (שלוש שורות בתחתית הקובץ ואחת ב-head). בלי זה הדפדפן של עומרי ימשיך להריץ
  את הגרסה הישנה מהמטמון. זו לא תיאוריה — זה קרה כאן בפיתוח.

## כתובות (routes) ומשמרות

לכל מסך יש כתובת, כדי שבבדיקות אפשר יהיה לקפוץ ישר לאן שצריך ושכפתור
"אחורה" יתנהג כמו "המסך הקודם" ולא כמו "צא מהאתר".

| כתובת | מה זה |
|---|---|
| `#/greeting` | הברכה |
| `#/countdown` | הספירה לאחור |
| `#/code` | הזנת קוד |
| `#/stop/N` | הסרטון של תחנה N (מתחיל מ-1) |
| `#/next/N` | הרמז לתחנה הבאה |
| `#/wrong` | סרטון "הקוד שגוי" |
| `#/finale` | מסך הסיום |

**כתובת עירומה** (בלי `#`) תמיד נכנסת דרך `#/greeting`. זו נקודת הכניסה,
וזה מה שעומרי יקבל כשילחץ על הלינק ששלחת לו.

**המקום נשמר.** הכתובת האחרונה נשמרת ב-`localStorage` יחד עם ההתקדמות:

- **רענון עם `#` בכתובת** — נשאר בדיוק באותו מסך, בלי לעבור דרך הברכה.
- **פתיחה של הלינק העירום באמצע המשחק** — הברכה רצה, ואז הוא חוזר *למסך
  המדויק* שהיה בו, לא להתחלה של המשחק.
- אם הכתובת השמורה כבר לא מותרת (למשל אופסה ההתקדמות) — נפילה רכה למסך
  הנכון, בלי שגיאה.

`#/greeting` ו-`#/wrong` לא נשמרים: הראשון הוא נקודת כניסה, השני רגע חולף
שאין טעם לשחזר.

**המשמר:** אף כתובת לא יכולה לקחת את עומרי קדימה. מקור האמת נשאר
`state.unlocked` ב-`localStorage`; הכתובת רק *מבקשת*, והמשמר מאשר או מחזיר
אותו למסך שהוא באמת אמור להיות בו. אם הוא יקליד `#/stop/6` באמצע המשחק —
הוא ינחת ב-`#/code`. `#/finale` נפתח רק אחרי שהתחנה האחרונה נוגנה.
כתובת שלא קיימת מתנהגת אותו דבר.

ההפניה נעשית ב-`replaceState`, כך שקפיצה חסומה לא משאירה רשומה מתה
בהיסטוריה — "אחורה" ממשיך לעבוד כרגיל אחריה.

זה נבדק: מהברכה כל קפיצה קדימה חוזרת לברכה; אחרי פתיחת התחנה הראשונה
`#/stop/1` מותר ו-`#/stop/2`, `#/stop/6` ו-`#/finale` חסומים; ואחרי סיום
המשחק שניהם נפתחים.

## מבנה

```
index.html    שלד
styles.css    עיצוב
app.js        לוגיקה
config.js     ← כל התוכן. זה מה שעורכים.
videos/       הסרטונים (placeholder כרגע)
posters/      תמונות פתיחה, אופציונלי
serve.py      שרת בדיקות מקומי (לא נדרש באוויר)
```
